version: "3.3"

services:
  # TODO: Criar uma imagem e container da aplicação...

  mongo:
    image: mongo:latest
    container_name: mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: PI-UFES-2022
    ports:
      - "27017:27017"
    network_mode: "host"
    volumes:
      - ./database-data:/data/db
